// =============================================================================
// Shared horizontal bounds for TopNav + (app) main — keep these in sync.
// =============================================================================

/** Max width + horizontal padding for all authenticated app surfaces. */
export const APP_SHELL_CLASS = "mx-auto w-full max-w-7xl px-4 sm:px-6";
